import { LimitSelection } from './limit-selection';

describe('LimitSelection', () => {
  it('should create an instance', () => {
    const directive = new LimitSelection();
    expect(directive).toBeTruthy();
  });
});
